import { McpServer } from "vite-plugin-mcp";
import { AstroIntegration } from "astro";

//#region src/index.d.ts
interface AstroMcpOptions {
  editor?: "cursor" | "vscode" | "windsurf";
}
declare function createAstroMcpIntegration({
  editor
}?: AstroMcpOptions): AstroIntegration;
declare global {
  namespace Astro {
    interface IntegrationHooks {
      "mcp:setup"?: (options: {
        mcp: McpServer;
      }) => void | Promise<void>;
    }
  }
}
//#endregion
export { createAstroMcpIntegration as default };